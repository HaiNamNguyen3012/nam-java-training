package broCode.OOP.Variable_scope;

public class Variable_scope {
    public static void main(String[] args){
        // local = declare inside a method and visible only to that method
        // global = declare outside a method, but within a class visible to all parts of the class

        DiceRoller  diceRoller = new DiceRoller();
    }
}

package broCode.OOP.abstraction;

public class Main {
    /*
     * abstract = abstract classes cannot be instantiated
     * , but they can have a subclass abstract method are
     * declared without an implementation
     */
    // Vehicle vehicle = new Vehicle();
    Car car = new Car();
}

package brocode.guiswing.aboutgui;

import java.awt.Color;

import javax.swing.ImageIcon;
import javax.swing.JFrame;

public class Main {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		// JFrame = a GUI window to add components to
		/*
		 * JFrame frame = new JFrame(); // creates a frame
		 * frame.setTitle("This is a JFrame title"); // sets title of frame
		 * frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); //exit out of application
		 * frame.setResizable(false); //prevent frame from being resized
		 * frame.setSize(420, 420); //set the x-dimension, y-dimension of frame
		 * frame.setVisible(true); //make frame visible
		 * ImageIcon image = new ImageIcon("src\\brocode\\aboutgui\\329589972_496697295999587_2060739924983275244_n.png");
		 * frame.setIconImage(image.getImage());
		 * frame.getContentPane().setBackground(new Color(123,50,250));
		 */
		 MyFrame myFrame = new MyFrame();
	}

}
